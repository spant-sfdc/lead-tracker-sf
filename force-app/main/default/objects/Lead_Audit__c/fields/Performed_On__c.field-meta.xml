<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Performed_On__c</fullName>
    <description>The exact date and time this event occurred in the user's local timezone context. Set via System.now() in the service layer. Used to build the chronological audit timeline and to calculate SLA elapsed time between stage transitions.</description>
    <externalId>false</externalId>
    <inlineHelpText>When this event occurred. Used to build the audit timeline and measure time between stages.</inlineHelpText>
    <label>Performed On</label>
    <required>true</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>DateTime</type>
</CustomField>
